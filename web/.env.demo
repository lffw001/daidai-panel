# 在线演示 Demo 构建（npm run build:demo，即 vite build --mode demo）专用。
#
# VITE_DEMO=1 是 web/src/demo/** 唯一的挂载开关：
#   - 只有它等于 '1' 时，main.ts 才会动态 import 并安装浏览器内 mock 层；
#   - 发布版构建（默认 production 模式）不加载本文件，该常量在编译期折叠成 ''，
#     整个 demo 分支连同 chunk 会被 rollup 剔除（发布版产物必须 0 字节 demo 代码）。
#
# 用 .env.demo 而不是在 npm script 里写 `VITE_DEMO=1 vite build`：
# 后者在 Windows 的 cmd/PowerShell 下不是合法语法，本地根本跑不起来。
#
# 注意：这里【绝对不要】加 VITE_API_BASE。
# web/src/views/subscriptions/index.vue 是全站唯一读取它的地方，
# 一旦注入，订阅页的 SSE 路径会整体失配。
VITE_DEMO=1
